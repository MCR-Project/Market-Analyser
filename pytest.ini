[pytest]
# backend/tests covers backend/ (FastAPI service + scripts/); tests/ covers
# fetcher/ (provider holdings parsers) - see tests/conftest.py for how the
# latter gets fetcher/ onto sys.path, the same way each fetcher module
# expects when run directly as a script.
testpaths =
    backend/tests
    tests
