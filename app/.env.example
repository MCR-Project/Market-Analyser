# Base URL the frontend sends API requests to, including the /api prefix.
# Defaults to http://localhost:8000/api when unset (see src/utils/api.js) -
# only needed to point a build at a non-localhost backend.
VITE_API_BASE=http://localhost:8000/api
